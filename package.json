{
  "name": "arcana",
  "version": "0.0.0",
  "private": true,
  "description": "Arcana — graph-native multi-agent research and learning platform (FYP).",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "codegen": "pnpm --filter @arcana/schema codegen",
    "codegen:check": "pnpm --filter @arcana/schema codegen:check"
  }
}
